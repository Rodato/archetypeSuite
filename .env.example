# OpenRouter API key — obtén una en https://openrouter.ai/keys
# Requerida para que el pipeline pueda invocar los modelos LLM (Claude, Grok).
OPENROUTER_API_KEY=sk-or-v1-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Opcionales — descomenta y ajusta solo si quieres sobreescribir defaults.
# LLM_MODEL=anthropic/claude-sonnet-4.5
# LLM_NARRATIVE_MODEL=x-ai/grok-4.3
# LLM_TEMPERATURE=0.0
# LLM_REQUEST_TIMEOUT=60
# MAX_REFINEMENT_ITERATIONS=2
# K_OPTIMIZER_MAX=10

# --- API (FastAPI) ---
# Orígenes permitidos por CORS (coma-separados, o "*" para todos). Default "*".
# ARCHETYPE_CORS_ORIGINS=http://localhost:3000
# Carpeta donde se persisten los análisis ("Mis análisis"). Default api/_data.
# ARCHETYPE_DATA_DIR=./api/_data
